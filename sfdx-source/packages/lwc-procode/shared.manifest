# Which of sfdx-source/shared/ is assembled into this package at build time.
# Values: all | none   (see scripts/create-package-versions.sh)
#
# paymentForm imports c/currencyUtils and renders <c-currency-picker>. (Amount/progress components are unused here, but assembly is all-or-nothing for now.)
all
